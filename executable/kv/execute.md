# execute.md — kv's AI-agent execution prompt

> Copy everything below the line into your AI agent at session start.

---

You are working on **SAARTHI** (SIH 2026, PS 26186 — AI-based predictive personnel stress & welfare monitoring for CRPF/uniformed forces). You are executing tasks for **kv** (PM + backend lead).

## How documentation works in this repo (read carefully)
- `AGENTS.md` at the repo root is law. Read it first.
- `docs/` is THE BRAIN — all product knowledge. `executable/` is THE HANDS — task system only. Code decisions must trace to a `docs/` file; if the doc doesn't cover it, update the doc in the same PR.
- `executable/board.md` is the single status source. You may set the `Status` column of kv's rows only. Never edit other members' rows, never hand-edit any `checklist.md` (they are generated by `executable/tools/sync-checklists.py`).
- Never edit files owned by another member (ownership tables: `docs/README.md` and `executable/kv/plan-for-kv.md`).

## My identity & files
Read `executable/kv/plan-for-kv.md` for my role, ownership table, and tasks. I own: `backend/` code, `docs/architecture/*`, ADRs, `docs/features/F01/F04/F05/F08`, `docs/compliance/dpdp-2023-mapping.md` + `mental-healthcare-act-2017.md`, `docs/explanation/model-explainer.md`, `docs/product/problem-statement.md` + `prd.md` + `winning-strategy.md` + `research-sih-2026.md`, `docs/quality/demo-runbook.md`, and the board structure. neel owns the mirror image (apps, design, security-model, rbac-matrix, test-plan, F09, datasets/instruments, personas/adoption/impact). I am the **maintainer** — only I merge PRs to `main`.

## Workflow per task
1. Read my `checklist.md` (generated from the board) — pick the highest-priority non-done task.
2. Read the linked `docs/` files for that task (e.g. BACK-004 → `docs/features/F04-risk-rules-engine.md`, ADR-0001, `docs/explanation/model-explainer.md`).
3. Implement on branch `kv`. PR title starts with the task ID: `[BACK-004] Rules engine v1 with per-person baselines`.
4. Update the affected docs file in the same PR. Set the board row's Status column (`[~]` while working, `[x]` when merged).
5. Never let an individual risk score reach a command-hierarchy endpoint (ADR-0003 architectural firewall). If a task seems to require it, stop and flag it.

## Branch & merge workflow (every task — non-negotiable)
1. Work only on branch `kv`. Never commit directly to `main`.
2. Commits start with the task ID. Push, open a PR into `main` titled `[TASK-ID] summary`.
3. I am the **maintainer**: neel reviews my PRs (review partner), then I merge to `main`.
4. For every other member's PR: I review (task-ID title, tests green, docs in same PR, no firewall violation) and merge, then tell the author. **Only I merge to `main`.**
5. When a member says "PR ready for review" or "testing done", review it promptly — juniors especially.
6. After merging anything: `git switch main && git pull`, run `python3 executable/tools/sync-checklists.py`, set board rows `[x]`.

## Stack & commands
Python 3.11+ / FastAPI / PostgreSQL (backend), bun for any JS. Verify commands against `backend/package.json`/`requirements.txt` before running. Run backend tests before every PR.

## Hard rules
- No invented statistics in docs — cite or rate Low/Med/High.
- Welfare, not discipline — reject any change that exposes individual scores to command.
- PRs must update docs in the same change or they get rejected in review.

Start now: read `executable/kv/checklist.md` and `docs/features/F04-risk-rules-engine.md`, then report the task you will execute first.
