# execute.md — neel's AI-agent execution prompt

> Copy everything below the line into your AI agent at session start.

---

You are working on **SAARTHI** (SIH 2026, PS 26186 — AI-based predictive personnel stress & welfare monitoring for CRPF/uniformed forces). You are executing tasks for **neel** (client & design lead).

## How documentation works in this repo (read carefully)
- `AGENTS.md` at the repo root is law. Read it first.
- **`global_instructions.md` at the repo root is the global operating manual** — master roadmap, complete work progress checklist, global rules, and the startup order for every session. Read it second.
- `docs/` is THE BRAIN — all product knowledge. `executable/` is THE HANDS — task system only. Code decisions must trace to a `docs/` file; if the doc doesn't cover it, update the doc in the same PR.
- `executable/board.md` is the single status source. You may set the `Status` column of neel's rows only. Never hand-edit any `checklist.md` (generated by `executable/tools/sync-checklists.py`).
- Never edit files owned by another member (ownership tables: `docs/README.md` and `executable/neel/plan-for-neel.md`).

## My identity & files
Read `executable/neel/plan-for-neel.md` for my role, ownership table, and tasks. I am **co-backbone with kv** (equal split). I own: `mobile/` + `web/` + `data/` code, the design system (`docs/architecture/design/`), feature docs F02, F03, F06, F07, F09, `docs/compliance/security-model.md` + `rbac-matrix.md`, `docs/explanation/datasets-research.md` + `clinical-instruments.md`, `docs/product/personas.md` + `adoption-strategy.md` + `impact-and-metrics.md`, `docs/quality/test-plan.md` + `usability-testing.md`.

## Workflow per task
1. Read my `checklist.md` (generated from the board) — pick the highest-priority non-done task.
2. Read the linked `docs/` files for that task (e.g. APP-005 → `docs/features/F02-jawan-app.md` + ADR-0003 + `docs/compliance/rbac-matrix.md`).
3. Implement on branch `neel`. PR title starts with the task ID: `[APP-005] who-viewed-my-data screen`.
4. Update the affected docs file in the same PR. Set the board row's Status column (`[~]` while working, `[x]` when merged).

## Branch & merge workflow (every task — non-negotiable)
1. Work only on branch `neel`. Never commit directly to `main`.
2. Commits start with the task ID. Push, open a PR into `main` titled `[TASK-ID] summary`.
3. **Notify kv (the maintainer): "PR ready for review."** Only kv merges to `main`. Never merge my own PR.
4. After kv merges: `git switch main && git pull`, set the board row `[x]`.

## Design constraints (non-negotiable)
- Offline-first (ADR-0005): check-ins must capture without network; SQLite queue + resumable sync.
- Low-end Android (API 26+), icon-first UI, voice input option, mixed-literacy safe. Hindi + English via i18n keys — no hardcoded strings, ever.
- The app's home is the roster app (ADR-0004): roster/leave/pay-slip screens first, check-in embedded there. Never brand it as a therapy app — "fitness for duty + parivaar welfare".
- No facial emotion recognition, no phone monitoring (F08 anti-goals).
- Commander dashboard renders aggregates only (k ≥ 5) — the UI must not even render individual rows.
- Stack: Expo / React Native (mobile), Next.js with bun (web).

Start now: read `executable/neel/checklist.md` and `docs/architecture/design/design.md`, then report the task you will execute first.
