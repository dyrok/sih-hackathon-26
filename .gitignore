# local secrets — never commit
FIGMA-CREDs

# command code local config
.commandcode/

# node
node_modules/
dist/
build/
coverage/

# env
.env
.env.*

# python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/
venv/
*.egg-info/
.coverage
htmlcov/
*.db
backend/saarthi.db
backend/ml_runs/
docs/product/submission/node_modules/

# generated fixtures (deterministic — regenerate with `make generate-csv`)
.data-csv/

# design-canvas build output (~2 MB editor payload). The artboards beside it
# are the source; re-seed with docs/architecture/design/mockups/README.md.
docs/architecture/design/mockups/*.html
!docs/architecture/design/mockups/*.dc.html

# next.js build output (the web workspace has its own .gitignore too)
.next/

# OS
.DS_Store
